import React from "react";
import ReactDOM from "react-dom/client";

const heading=React.createElement('h1',{},'SAICHARAN');
const root= ReactDOM.createRoot(document.getElementById('left'));
root.render(heading);